# Gate-Wize Backend

Minimal Express server with TypeScript.

## Scripts

- `npm run dev`: Run dev server with reload
- `npm run build`: Compile TypeScript to `dist`
- `npm start`: Start compiled server

## Env

- `PORT` (default: 4000)

## Healthcheck

GET `http://localhost:4000/health`
